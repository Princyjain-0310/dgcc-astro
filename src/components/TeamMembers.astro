---
import {Icon} from 'astro-icon/components'
const {name, image, bio} = Astro.props.data;
const image_dimension = 246;
---
<div class="mb-8 md:mb-0 w-full h-full  group/teamMemberCard relative font-normal">
    <!--border-dashed border-8 border-gray-900 hover:border-gray-300-->
    <!--<img class="w-full h-fit rounded-t-2xl aspect-square" src={image} alt={name}/>-->
    <!--<div class="w-full text-center font-extrabold text-white bg-gray-900 p-2">-->
    <!--    <div class="mb-2">{name}</div>-->
    <!--    <hr/>-->
    <!--    <div class="group-hover/teamMemberCard:text-red-400 ">{bio}</div>-->
    <!--</div>-->
    <!--<div class="group-hover:block hidden">-->

    <!--</div>-->

    <img src={image} class="object-cover aspect-square w-full h-full rounded-t-xl" alt={image}/>
    <div id="overlay" class="w-full flex flex-col absolute bottom-0 transition-all ease-in-out duration-200">
        <span class="w-full text-center bg-gray-900 p-2">{name}</span>
        <p class="w-full p-4 hidden opacity-0 group-hover/teamMemberCard:block bg-gray-900 group-hover/teamMemberCard:opacity-95 fade delay-200 duration-300 ease-in-out text-gray-400">
            {bio}
        </p>
    </div>
</div>